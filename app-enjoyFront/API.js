import { URI_HOST, URI_LOGIN, _BODY } from './APIconfig';
const ERROR_INVALID_TOKEN = { error: 'Invalid token' };
const ERROR_LOGIN = { error: 'Invalid login' };
const ERROR_LOADING = { error: 'Error loading' };


export default { API };
